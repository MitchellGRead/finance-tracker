export const GLOBAL_DEFAULT_SPLIT_RATIO = 0.5;
export const PERSONAL_SPLIT_RATIO = 1.0;

/** Split ratios are floats; compare them with this tolerance, never with ===. */
export const SPLIT_RATIO_EPSILON = 0.001;

/** Suggestions at or above this confidence render at full strength. */
export const SUGGESTION_CONFIDENCE_TIER = 0.75;
